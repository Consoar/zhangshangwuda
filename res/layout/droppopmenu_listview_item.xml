<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:descendantFocusability="blocksDescendants"
    android:minHeight="40dip"
    android:orientation="horizontal"
    android:paddingLeft="20dp" >

    <TextView
        android:id="@+id/droppopmenu_listView_item_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:maxLines="1"
        android:textColor="?attr/newsItemTextColor"
        android:textSize="16sp" />

    <ImageButton
        android:id="@+id/droppopmenu_listView_item_delete"
        android:layout_width="40dip"
        android:layout_height="40dip"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:background="@drawable/list_item_bg"
        android:focusable="false"
        android:scaleType="centerInside"
        android:src="?attr/droppopmenu_delete_BG" />

    <TextView
        android:id="@+id/droppopmenu_listView_item_id"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:focusable="false"
        android:visibility="gone" />

</RelativeLayout>